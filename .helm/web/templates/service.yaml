apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.web.port | int  }}
      targetPort: {{ .Values.web.port | int  }}
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP