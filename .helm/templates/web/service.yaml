apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-web"
  labels:
    app: "{{ .Release.Name }}-web"
spec:
  ports:
    - protocol: TCP
      port: "{{ .Values.web.port }}"
      targetPort: "{{ .Values.web.port }}"
  selector:
    app: "{{ .Release.Name }}-web"
  type: ClusterIP