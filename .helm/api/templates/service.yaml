apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels:
    app: {{ .Release.Name }}
spec:
  ports:
    - protocol: TCP
      port: {{ .Values.api.port | int }}
      targetPort: {{ .Values.api.port | int  }}
  selector:
    app: {{ .Release.Name }}
  type: ClusterIP