name: Release api

on:
  push:
    branches:
      - release/all**
      - release/api**
    tags:
      - release/all**
      - release/api**

jobs:
  build-and-deploy:
    uses: chmonya-inc/dnd-manager/.github/workflows/build-deploy-docker.yml@cicd
    with:
      image-name: chmonya777/dndmanager-api
      dockerfile-path: dockerfiles/api.Dockerfile
      release-name: dndmanager-api
      k8s-namespace: dndmanager
    secrets: inherit
