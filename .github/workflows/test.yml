name: Release api

on:
  pull_request:
    branches:
      - master

jobs:
  manual-approval:
    uses: chmonya-inc/dnd-manager/.github/workflows/common.yml@cicd
    with:
      app-name: Test
    secrets: inherit